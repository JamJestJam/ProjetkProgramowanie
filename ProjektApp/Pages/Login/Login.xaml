<UserControl x:Class="ProjektApp.Pages.Login.Login"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" 
             xmlns:local="clr-namespace:ProjektApp.Pages.Login"
             mc:Ignorable="d" 
             d:DesignHeight="570" d:DesignWidth="724">
    <materialDesign:DialogHost x:Name="Loading" IsOpen="False">
        <materialDesign:DialogHost.DialogContent>
            <ProgressBar Style="{DynamicResource MaterialDesignCircularProgressBar}" HorizontalAlignment="Center" Margin="16" IsIndeterminate="True" />
        </materialDesign:DialogHost.DialogContent>
        <materialDesign:DialogHost x:Name="Dialog" IsOpen="False" CloseOnClickAway="True" MinWidth="400">
            <materialDesign:DialogHost.DialogContent>
                <StackPanel Margin="16">
                    <Label Content="" x:Name="DialogText" FontSize="30" HorizontalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button IsDefault="True" Margin="0 8 8 0" HorizontalAlignment="Center" Content="Rozumiem"  Click="CloseDialog"/>
                    </StackPanel>
                </StackPanel>
            </materialDesign:DialogHost.DialogContent>
            <Border Background="#333" CornerRadius="25" Margin="20" Width="350" Height="300">
                <StackPanel Margin="20">
                    <Label Content="Zaloguj się" FontSize="25" HorizontalAlignment="Center" />
                    <Separator />
                    <Label Content="Nazwa użytkownika"/>
                    <TextBox x:Name="UserName" FontSize="18" Background="#444" Foreground="#fff"/>
                    <Label Content="Hasło"/>
                    <PasswordBox x:Name="Password" Background="#444" FontSize="18"/>
                    <Button Content="Zaloguj się" Background="#444" Height="50" FontSize="18" Margin="20" Click="LoginIn" Style="{StaticResource MaterialDesignFlatButton}"/>
                </StackPanel>
            </Border>
        </materialDesign:DialogHost>
    </materialDesign:DialogHost>
</UserControl>
